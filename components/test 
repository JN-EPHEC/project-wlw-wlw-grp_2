import React, { useState } from 'react';
import { View, Text, Image, Pressable, StyleSheet } from 'react-native';

interface SkillCardProps {
  userName: string;
  userImage: string;
  skillOffered: string;
  skillWanted: string;
  description: string;
}

const SkillCard: React.FC<SkillCardProps> = ({
  userName,
  userImage,
  skillOffered,
  skillWanted,
  description,
}) => {
  const [isLiked, setIsLiked] = useState(false);

  return (
    <View style={styles.card}>
      {/* En-tête avec photo de profil */}
      <View style={styles.header}>
        <Image source={{ uri: userImage }} style={styles.avatar} />
        <Text style={styles.userName}>@{userName}</Text>
      </View>

      {/* Échange de compétences */}
      <View style={styles.skillsRow}>
        <View style={styles.skillBox}>
          <Text style={styles.label}>J'offre 🎁</Text>
          <Text style={styles.skill}>{skillOffered}</Text>
        </View>
        
        <Text style={styles.arrow}>⇄</Text>
        
        <View style={[styles.skillBox, styles.wantedBox]}>
          <Text style={styles.label}>Je cherche 🔍</Text>
          <Text style={styles.skill}>{skillWanted}</Text>
        </View>
      </View>

      {/* Description */}
      <Text style={styles.description}>{description}</Text>

      {/* Boutons d'action */}
      <View style={styles.actions}>
        <Pressable 
          style={styles.likeButton}
          onPress={() => setIsLiked(!isLiked)}
        >
          <Text style={styles.buttonText}>
            {isLiked ? '❤️ Intéressé' : '🤍 Intéresse'}
          </Text>
        </Pressable>

        <Pressable style={styles.contactButton}>
          <Text style={styles.contactText}>💬 Contacter</Text>
        </Pressable>
      </View>
    </View>
  );
};

const styles = StyleSheet.create({
  card: {
    backgroundColor: '#fff',
    borderRadius: 20,
    padding: 20,
    margin: 15,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.15,
    shadowRadius: 10,
    elevation: 8,
  },
  header: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 20,
  },
  avatar: {
    width: 50,
    height: 50,
    borderRadius: 25,
    marginRight: 12,
  },
  userName: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#333',
  },
  skillsRow: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 15,
  },
  skillBox: {
    flex: 1,
    backgroundColor: '#4CAF50',
    borderRadius: 12,
    padding: 12,
    alignItems: 'center',
  },
  wantedBox: {
    backgroundColor: '#2196F3',
  },
  label: {
    fontSize: 12,
    color: '#fff',
    marginBottom: 5,
  },
  skill: {
    fontSize: 14,
    fontWeight: 'bold',
    color: '#fff',
    textAlign: 'center',
  },
  arrow: {
    fontSize: 24,
    marginHorizontal: 10,
    color: '#666',
  },
  description: {
    fontSize: 15,
    color: '#666',
    lineHeight: 22,
    marginBottom: 20,
  },
  actions: {
    flexDirection: 'row',
    gap: 10,
  },
  likeButton: {
    flex: 1,
    backgroundColor: '#f0f0f0',
    borderRadius: 25,
    padding: 15,
    alignItems: 'center',
  },
  contactButton: {
    flex: 1,
    backgroundColor: '#4dabf7',
    borderRadius: 25,
    padding: 15,
    alignItems: 'center',
  },
  buttonText: {
    fontSize: 15,
    fontWeight: '600',
    color: '#333',
  },
  contactText: {
    fontSize: 15,
    fontWeight: 'bold',
    color: '#fff',
  },
});

export default SkillCard;